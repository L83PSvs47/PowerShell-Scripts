# PowerShell-Scripts

Simple PowerShell scripts and modules for system administrators.

Add modules to the path specified by the environment variable `$env:PSModulePath`, or import using the command:

```powershell
Import-Module -Name "$PSScriptRoot\modules\ModuleName"
```
